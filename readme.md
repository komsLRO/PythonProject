# Telegram Planner Bot

Бот-планировщик для Telegram с использованием ИИ для поиска похожих задач.

## Функционал

- Добавление задач с датой и временем
- Просмотр задач на сегодня
- Просмотр задач на неделю вперед
- Отметка задач как выполненные/невыполненные
- Удаление задач
- Поиск похожих задач по смыслу с использованием эмбеддингов

## Команды

- `/start` - запуск бота и краткая справка
- `/help` - подробная справка со всеми командами
- `/add` - добавить задачу (интерактивный диалог)
- `/today` - задачи на сегодня
- `/week` - задачи на неделю
- `/list` - все задачи пользователя
- `/cleanup` - удалить все просроченные задачи
- `/clear_all` - удалить ВСЕ задачи (с подтверждением)
- `/reset_ids` - сбросить счетчик ID задач
- `/done N` - отметить задачу выполненной (N - номер по времени)
- `/undo N` - отменить выполнение задачи
- `/delete N` - удалить задачу
- `/search запрос` - найти похожие задачи по смыслу
- `/cancel` - отменить текущую операцию

## Добавление задач

Команда `/add` запускает интерактивный диалог:

1. **Название задачи** - введите текст задачи
2. **Дата** - укажите когда выполнить
3. **Время** - укажите во сколько выполнить

### Форматы дат
- `сегодня`, `завтра`, `послезавтра`
- `+1`, `+2` (дни от сегодня)
- `25.12`, `25.12.2024`
- `2024-12-25`

### Форматы времени
- `14:30`, `14.30`
- `утро` (09:00), `день` (12:00), `вечер` (18:00), `ночь` (22:00)
- `сейчас`

### Пример диалога
```
Вы: /add
Бот: 📝 Добавление новой задачи
     Введите название задачи:
     (или /cancel для отмены)

Вы: Сделать домашнее задание
Бот: ✅ Название: Сделать домашнее задание
     Теперь введите дату и время в формате:
     DD.MM.YYYY HH:MM
     Примеры:
     • 25.12.2024 14:30
     • 01.01.2025 09:00
     (или /cancel для отмены)

Вы: 25.12.2024 15:00
Бот: ✅ Задача создана!
     ID: 123
     Задача: Сделать домашнее задание
     Когда: 25.12.2024 15:00
```

### Формат отображения
Все задачи отображаются в едином числовом формате:
- **Дата:** DD.MM.YYYY (например: 25.12.2024)
- **Время:** HH:MM (например: 14:30)
- **Полный формат:** DD.MM.YYYY HH:MM

### Нумерация задач
Задачи нумеруются **по порядку времени выполнения** для ВСЕХ задач пользователя:
- **1** - самая ранняя задача (независимо от даты)
- **2** - следующая по времени
- **3** - следующая и т.д.

Это позволяет работать с задачами по их временному порядку ВНЕЗАВИСИМО от того, в каком списке они отображаются.

Пример нумерации:
```
📅 Задачи на 17.12.2024:
1. [17.12.2024 15:00] Встреча с клиентом ⏳
2. [17.12.2024 17:30] Спортзал ⏳

📋 Все задачи:
1. [17.12.2024 15:00] Встреча с клиентом ⏳
2. [17.12.2024 17:30] Спортзал ⏳
3. [18.12.2024 10:00] Совещание ⏳
```

Команды `/done 1`, `/undo 2`, `/delete 3` работают одинаково во всех списках!

### Отмена операции
В любой момент диалога можно отправить `/cancel` для отмены добавления задачи.

## Удаление всех задач

Команда `/clear_all` позволяет удалить все задачи пользователя, но требует подтверждения:

### Пример использования
```
Вы: /clear_all
Бот: ⚠️ ВНИМАНИЕ!
     Вы собираетесь удалить ВСЕ 5 задач!
     Это действие НЕЛЬЗЯ отменить!
     Для подтверждения введите: ДА УДАЛИТЬ ВСЕ
     Для отмены введите: НЕТ или /cancel

Вы: ДА УДАЛИТЬ ВСЕ
Бот: 🗑️ ВСЕ ЗАДАЧИ УДАЛЕНЫ!
     Удалено задач: 5

Вы: НЕТ
Бот: ✅ Удаление отменено.
```

## Сброс ID счетчика

Команда `/reset_ids` оптимизирует нумерацию ID задач:

- **Если задач нет** - сбрасывает счетчик на 1
- **Если задачи есть** - устанавливает счетчик на следующий после максимального ID
- **Результат** - новые задачи будут иметь последовательные ID

### Когда использовать
После массового удаления задач ID могут идти не по порядку (1, 5, 12, 15...). Команда `/reset_ids` исправляет это.

### Пример
```
До: задачи с ID 1, 5, 12, 15
После /reset_ids: счетчик готов для ID 16 (следующая задача)
```

## Автоматическая очистка

Бот автоматически удаляет просроченные задачи:

- **При запуске бота** - очищает все просроченные задачи
- **При просмотре задач** - удаляет просроченные перед показом списков
- **Ручная очистка** - команда `/cleanup` для принудительного удаления

Это помогает поддерживать актуальность списка задач и избегать накопления старых невыполненных заданий.

## Структура проекта

- `config.py` - настройки бота
- `utils.py` - утилитные функции (работа с датами, эмбеддингами)
- `database.py` - функции для работы с базой данных
- `handlers.py` - обработчики команд Telegram
- `main.py` - точка входа в приложение

## Настройка

### Получение токена бота

1. Перейдите к [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### Настройка переменной окружения

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_BOT_TOKEN = "ваш_токен_здесь"
```

**Windows (cmd):**
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

**Linux/macOS:**
```bash
export TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

### Альтернативный способ

Можно напрямую изменить `TOKEN` в файле `config.py`, но это менее безопасно.

Также можно создать файл `.env` на основе `env_example.txt`.

## Зависимости

- aiogram
- aiosqlite
- numpy
- sentence-transformers

## Запуск

### 1. Установка переменной окружения
```bash
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN = "ваш_токен_здесь"

# Linux/macOS
export TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

### 2. Запуск бота
```bash
# Рекомендуемый способ - из корневой директории проекта
python run_bot.py

# Альтернативный способ - из директории бота
cd tg_planer_aiogram
python main.py
```

### 3. Проверка работы
Если бот запустился успешно, вы увидите сообщение:
```
Запуск бота из директории: C:\path\to\project\tg_planer_aiogram
INFO | planner_bot | Бот успешно авторизован: @your_bot_username (ID: 123456789)
INFO | planner_bot | База данных инициализирована
INFO | planner_bot | Бот запускается...
```

### 4. Диагностика проблем
Для автоматической диагностики запустите:
```bash
python check_and_run.py
```

Этот скрипт проверит все компоненты и покажет конкретные ошибки.

## Диагностика проблем

### Проблема: "Переменная окружения TELEGRAM_BOT_TOKEN не установлена"
**Решение:**
```powershell
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN = "7974624776:AAESUpg1b2EB_l6VUbWPIsosCOzHG98wfUs"
```

### Проблема: "Conflict: terminated by other getUpdates request"
**Решение:**
```bash
# Автоматическое решение (рекомендуется):
python check_and_run.py

# Или ручное:
python stop_bot.py
python check_and_run.py
```

**Бот автоматически:**
1. Обнаруживает конфликт
2. Останавливает другие процессы
3. Перезапускается через 3 секунды

### Проблема: "Ошибка авторизации бота"
**Решение:**
1. Проверьте правильность токена у [@BotFather](https://t.me/BotFather)
2. Убедитесь, что токен не истек
3. Проверьте, что бот не заблокирован Telegram
4. Проверьте интернет-соединение

### Проблема: "Директория бота не найдена"
**Решение:** Запускайте скрипт из корневой директории проекта

### Проблема: "Ошибка импорта модулей"
**Решение:** Убедитесь, что установлены зависимости:
```bash
pip install aiogram aiosqlite numpy sentence-transformers
```